# Browse repositories

Use these tools to explore CodeCommit repositories:

1. `list_repos` — see all available repositories
2. `list_files` with `repo` and optional `path` — browse directory contents
3. `get_file` with `repo` and `path` — read file content

Always specify the `ref` (branch) when reading files from non-default branches.
