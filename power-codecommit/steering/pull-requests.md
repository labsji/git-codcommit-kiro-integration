# Pull requests

- `list_prs` — show open PRs (or pass `state: "CLOSED"`)
- `create_pr` — open a new PR after pushing a branch

## Workflow
1. Create feature branch from main
2. Commit changes to the feature branch
3. Create PR: source = feature branch, target = main
4. Share PR ID for review
